# 🛠️ منصة الإعدادات (Settings Platform)

أهلاً بك يا مالك التطبيق. هذا الدليل مُصمم خصيصاً لك لتتمكن من تغيير تفضيلات التطبيق الأساسية (الألوان، الخصوصية، السلوك) مباشرة في الكود البرمجي دون الحاجة لخبرة برمجية واسعة.

---

### 📂 الملف الأساسي للإعدادات
معظم التعديلات التي تهمك موجودة في ملف واحد مركزي:
**المسار:** `lib/core/constants/app_config.dart`

---

### 📝 قائمة التعديلات المتاحة

#### 001 - تغيير الخصوصية الافتراضية للمستخدم الجديد
إذا أردت أن يكون أي حساب جديد "خاصاً" تلقائياً بحيث لا يراه أحد إلا بموافقة:
1. اذهب للملف `lib/core/constants/app_config.dart`
2. ابحث عن السطر: `static const bool defaultUserIsPublic = ...`
3. غير القيمة إلى `false` (للحسابات الخاصة) أو `true` (للحسابات العامة).

#### 002 - إغلاق أو فتح باب التسجيل
إذا أردت إيقاف استقبال مستخدمين جدد مؤقتاً:
1. اذهب للملف `lib/core/constants/app_config.dart`
2. ابحث عن السطر: `static const bool isRegistrationEnabled = ...`
3. غير القيمة إلى `false` لإغلاق التسجيل، أو `true` لفتحه.

#### 003 - التحكم في ظهور الإدمن (Admin) في البحث
1. اذهب للملف `lib/core/constants/app_config.dart`
2. ابحث عن السطر: `static const bool showAdminsInSearch = ...`
3. اختر `true` للظهور أو `false` للإخفاء.

#### 004 - تغيير سرعة نبض الصورة الشخصية (Avatar)
1. اذهب للملف `lib/core/constants/app_config.dart`
2. ابحث عن السطر: `static const int avatarPulseDurationMs = 3000;`
3. الرقم (3000) يعني 3 ثوانٍ. إذا أردت نبضاً أسرع، صغّر الرقم (مثلاً 1500).

#### 005 - تغيير الألوان الأساسية للتطبيق
**المسار:** `lib/core/constants/app_colors.dart`
1. ابحث عن `static const Color primary = ...`
2. يمكنك تغيير الكود اللوني (مثلاً `0xFF2196F3` للأزرق).

---

### ⚠️ ملاحظات هامة
- **الكلمات المفتاحية:** `true` تعني (نعم/تفعيل)، و `false` تعني (لا/إيقاف).
- **الفاصلة المنقوطة:** تأكد دائماً من وجود `;` في نهاية كل سطر تقوم بتعديله.
- **الحفظ:** بعد التعديل، يجب حفظ الملف لكي يظهر التأثير في التطبيق.
